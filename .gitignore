# 渠道真实值：含 token，绝不进 git。线上由 CI Secret 注入，本地由此文件提供。
# 只跟踪 channel.env.example（示例，无真实值）。
/brand/channel.env

# 渲染产物：由 render-config.sh 把模板占位符替换成真实值生成，含 token，不进 git。
/brand/config.toml
/dist/

# 本地工作/编辑器文件
.chief/
.claude/
