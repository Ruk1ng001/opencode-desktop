# 补丁分组清单：定义每个补丁包含哪些 opencode 源码文件（相对 opencode/ 的路径）。
# 格式：每段以 [补丁名] 开头，下面列出属于该补丁的文件路径，一行一个。
# 按 NN- 前缀顺序应用到干净的上游基线（brand/BASE_SHA）上。
#
# 为什么用补丁层：opencode 以 git submodule 锁定在上游 tag，gitlink 必须保持干净
# 才能干净跟随上游 release。凡是必须改动 submodule 内源码的定制，都导出为本目录下
# 的 .patch，由 CI / 本机在 checkout 后重放，做到「submodule 锁上游 + 薄叠加层」。
#
# 应用（在 opencode/ 内，基线为 brand/BASE_SHA）：
#   git -C opencode apply --3way brand/patches/01-balance-panel.patch
# 导出（改动 opencode/ 后，在 opencode/ 内）：
#   git -C opencode diff -- <文件...> > brand/patches/NN-<name>.patch

# 独立余额面板（US-005 骨架 → US-006 接入 new-api 真实余额/用量）：
#   - 自包含面板组件（余额 / 已用量 / 充值，中文，仅依赖 solid-js）；
#   - 在渲染入口挂载独立悬浮入口，位于聊天 UI 树之外，不改动任何既有聊天组件；
#   - US-006 起：打开面板时向 new-api GET /api/user/self 拉取真实 quota/used_quota，
#     解析并做单位换算，含加载中 / 请求失败 / 未配置凭据 / 就绪四态。
[01-balance-panel]
packages/desktop/src/renderer/balance/balance-panel.tsx
packages/desktop/src/renderer/index.tsx

# 余额面板凭据注入（US-006）：
#   - 构建期把 US-003 注入的同名渠道变量（NEWAPI_BASE_URL / NEWAPI_API_KEY）经
#     electron.vite renderer.define 透传到 renderer 的 import.meta.env（浏览器上下文
#     读不到 process.env）；未注入时为空串，面板显示「未配置凭据」。不硬编码任何值。
#   - env.d.ts 补上对应的 ImportMetaEnv 类型声明，保证 typecheck 通过。
[02-balance-newapi]
packages/desktop/electron.vite.config.ts
packages/desktop/src/renderer/env.d.ts
