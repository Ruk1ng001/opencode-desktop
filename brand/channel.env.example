# 渠道真实值 —— 本地打包用（复制本文件为 brand/channel.env 并填入真实值）。
#
# ⚠️ brand/channel.env 含 token，已被根 .gitignore 忽略，绝不提交进 git。
#    线上打包时这些值由 CI Secret 以同名环境变量注入，同样不进 git。
#    渲染脚本读取优先级：进程环境变量（CI Secret） > 本文件。
#
# 三个变量对应 brand/opencode.template.json 里的三个占位符：
#     NEWAPI_BASE_URL → __BASE_URL__   渠道 API 地址（OpenAI 兼容端点）
#     NEWAPI_API_KEY  → __API_KEY__    渠道 bearer token / API key
#     NEWAPI_MODEL    → __MODEL__      默认模型名
#
# 想换渠道：改这里的值（本地）或改 CI Secret（线上），模板与源码都不用动。

NEWAPI_BASE_URL="https://your-newapi-host.example.com/v1"
NEWAPI_API_KEY="sk-your-real-token-here"
NEWAPI_MODEL="gpt-5-codex"
