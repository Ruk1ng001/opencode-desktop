# 渠道真实值 —— 本机验证 / 本地打包用（复制本文件为 brand/channel.env 并填入真实值）。
#
# ⚠️ brand/channel.env 含 token，已被根 .gitignore 忽略，绝不提交进 git。
#    线上打包时这些值由 CI Secret 以同名环境变量注入，同样不进 git、不进日志。
#
# 三个变量对应 brand/opencode.template.json 里的三处 opencode 原生 {env:VAR} 占位：
#     NEWAPI_BASE_URL → {env:NEWAPI_BASE_URL}   渠道 API 地址（OpenAI 兼容端点，命中 /v1/chat/completions）
#     NEWAPI_API_KEY  → {env:NEWAPI_API_KEY}    渠道 bearer token / API key
#     NEWAPI_MODEL    → {env:NEWAPI_MODEL}       默认模型名
#
# 本机验证（无需渲染脚本，opencode 运行时原生替换 {env:VAR}）：
#     set -a && . brand/channel.env && set +a
#     OPENCODE_CONFIG="$PWD/brand/opencode.template.json" opencode
#
# 想换渠道：改这里的值（本地）或改 CI Secret（线上），模板与源码都不用动。

NEWAPI_BASE_URL="https://your-newapi-host.example.com/v1"
NEWAPI_API_KEY="sk-your-real-token-here"
NEWAPI_MODEL="gpt-5-codex"
