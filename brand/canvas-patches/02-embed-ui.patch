diff --git a/web/src/components/layout/user-status-actions.tsx b/web/src/components/layout/user-status-actions.tsx
index 759307f..c478d51 100644
--- a/web/src/components/layout/user-status-actions.tsx
+++ b/web/src/components/layout/user-status-actions.tsx
@@ -1,11 +1,7 @@
 import type { CSSProperties } from "react";
 import { BookOpen, Keyboard, Puzzle, Settings2 } from "lucide-react";
 
-import { AnimatedThemeToggler } from "@/components/ui/animated-theme-toggler";
-import { GitHubLink } from "@/components/layout/github-link";
-import { VersionReleaseModal } from "@/components/layout/version-release-modal";
 import { DOCS_URL } from "@/constant/env";
-import { cn } from "@/lib/utils";
 import { canvasThemes } from "@/lib/canvas-theme";
 import { useConfigStore } from "@/stores/use-config-store";
 import { useThemeStore } from "@/stores/use-theme-store";
@@ -17,16 +13,15 @@ type UserStatusActionsProps = {
     onOpenPlugins?: () => void;
 };
 
+// [dokng] 顶栏精简：隐藏主题切换、版本显示、GitHub 三个入口（嵌入桌面壳内主题跟随宿主、
+// 版本/仓库标识对终端用户无意义）。仅保留文档、配置、节点插件、快捷键入口。
+// theme 仍从 store 读取供 variant="canvas" 的图标取色，只是不再暴露切换按钮。
 export function UserStatusActions({ showConfig = true, variant = "default", onOpenShortcuts, onOpenPlugins }: UserStatusActionsProps) {
     const theme = useThemeStore((state) => state.theme);
-    const setTheme = useThemeStore((state) => state.setTheme);
     const openConfigDialog = useConfigStore((state) => state.openConfigDialog);
     const canvasTheme = canvasThemes[theme];
     const naturalIconClass = "inline-flex size-7 shrink-0 items-center justify-center text-stone-600 transition hover:text-stone-950 dark:text-stone-300 dark:hover:text-white [&_svg]:size-4";
     const iconStyle: CSSProperties | undefined = variant === "canvas" ? { color: canvasTheme.node.text } : undefined;
-    const versionStyle = iconStyle;
-    const gitHubClassName = "size-7 text-base";
-    const gitHubStyle = iconStyle;
 
     return (
         <div className="inline-flex shrink-0 items-center gap-1">
@@ -43,9 +38,6 @@ export function UserStatusActions({ showConfig = true, variant = "default", onOp
                     <Settings2 className="size-4" />
                 </button>
             ) : null}
-            <AnimatedThemeToggler theme={theme} onThemeChange={setTheme} className={naturalIconClass} style={iconStyle} aria-label={theme === "dark" ? "切换到浅色主题" : "切换到深色主题"} title={theme === "dark" ? "切换到浅色主题" : "切换到深色主题"} />
-            <VersionReleaseModal style={versionStyle} />
-            <GitHubLink className={cn("bg-transparent hover:bg-transparent dark:hover:bg-transparent", gitHubClassName)} style={gitHubStyle} />
             {onOpenShortcuts ? (
                 <button type="button" className={naturalIconClass} style={iconStyle} onClick={onOpenShortcuts} aria-label="快捷键" title="快捷键">
                     <Keyboard className="size-4" />
diff --git a/web/src/pages/home/index.tsx b/web/src/pages/home/index.tsx
index 7007733..3d53d92 100644
--- a/web/src/pages/home/index.tsx
+++ b/web/src/pages/home/index.tsx
@@ -1,11 +1,9 @@
 import { ArrowRight } from "lucide-react";
-import { type ReactNode, useEffect, useState } from "react";
-import { App, Button, Image, Tag } from "antd";
+import { type ReactNode } from "react";
+import { Button } from "antd";
 import { useNavigate } from "react-router-dom";
 
-import { fetchPrompts, type Prompt } from "@/services/api/prompts";
 import { navigationTools } from "@/constant/navigation-tools";
-import { cn } from "@/lib/utils";
 
 function Highlighter({ action, color, children }: { action: "highlight" | "underline"; color: string; children: ReactNode }) {
     return (
@@ -20,27 +18,20 @@ function Highlighter({ action, color, children }: { action: "highlight" | "under
     );
 }
 
+// [dokng] 首页精简：只保留顶部 hero（标题 + 开始使用 / 打开画布），下方「沉淀每一次好结果」
+// 提示词宫格整块移除（图片来源不稳定、与画布主流程弱相关）。hero 容器拉满可用高度、垂直居中，
+// 布局伸缩铺满整个视口，不再受固定 min-height 约束。
 export default function IndexPage() {
-    const { message } = App.useApp();
     const navigate = useNavigate();
     const [primaryTool] = navigationTools;
-    const [promptShowcase, setPromptShowcase] = useState<Prompt[]>([]);
-    const [previewIndex, setPreviewIndex] = useState(0);
-    const [previewOpen, setPreviewOpen] = useState(false);
-
-    useEffect(() => {
-        void fetchPrompts({ pageSize: 12 })
-            .then((data) => setPromptShowcase(data.items))
-            .catch((error) => message.error(error instanceof Error ? error.message : "获取提示词失败"));
-    }, [message]);
 
     return (
         <main className="relative h-full overflow-y-auto bg-background bg-[radial-gradient(#e5e7eb_1px,transparent_1px)] [background-size:16px_16px] text-stone-950 dark:bg-[radial-gradient(rgba(245,245,244,.18)_1px,transparent_1px)] dark:text-stone-100">
-            <section className="relative mx-auto min-h-[calc(100vh-4rem)] max-w-7xl overflow-hidden px-6">
+            <section className="relative mx-auto flex h-full min-h-full max-w-7xl flex-col overflow-hidden px-6">
                 <div className="pointer-events-none absolute left-[15%] top-24 size-20 rounded-full border border-dashed border-stone-200 dark:border-stone-800" />
                 <div className="pointer-events-none absolute right-[23%] top-[48%] size-20 rounded-full border border-dashed border-stone-200 dark:border-stone-800" />
 
-                <div className="relative flex min-h-[620px] flex-col items-center justify-center pt-10 text-center">
+                <div className="relative flex flex-1 flex-col items-center justify-center text-center">
                     <h1 className="ai-title-aurora max-w-5xl text-balance text-5xl font-semibold tracking-normal sm:text-7xl lg:text-8xl">无限画布</h1>
                     <p className="mt-8 max-w-3xl text-balance text-lg leading-8 text-stone-500 dark:text-stone-400">
                         在
@@ -62,64 +53,7 @@ export default function IndexPage() {
                         </Button>
                     </div>
                 </div>
-
-                <section className="relative mx-auto mb-20 max-w-6xl border-t border-stone-200 pt-12 dark:border-stone-800">
-                    <div className="mb-8 grid gap-4 md:grid-cols-[1fr_auto_1fr] md:items-start">
-                        <div />
-                        <div className="max-w-2xl text-center">
-                            <h2 className="text-3xl font-semibold text-stone-950 dark:text-stone-100">沉淀每一次好结果</h2>
-                            <p className="mt-3 text-base leading-7 text-stone-500 dark:text-stone-400">收藏稳定出图的提示词、参考风格和结果图片，让下一次创作从已有经验开始。</p>
-                        </div>
-                        <Button type="link" onClick={() => navigate("/prompts")} className="justify-self-center md:justify-self-end" icon={<ArrowRight className="size-4" />} iconPlacement="end">
-                            查看提示词库
-                        </Button>
-                    </div>
-                    <div className="grid auto-rows-[210px] gap-4 md:grid-cols-4">
-                        {promptShowcase.map((item, index) => (
-                            <button
-                                key={item.id}
-                                type="button"
-                                onClick={() => {
-                                    setPreviewIndex(index);
-                                    setPreviewOpen(true);
-                                }}
-                                className={cn(
-                                    "group relative cursor-pointer overflow-hidden border border-stone-200 bg-stone-100 text-left dark:border-stone-800 dark:bg-stone-900",
-                                    index === 0 && "md:col-span-2 md:row-span-2",
-                                    index === 3 && "md:col-span-2",
-                                )}
-                            >
-                                <img src={item.coverUrl} alt={item.title} className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.03]" />
-                                <div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/70 via-black/35 to-transparent p-4 text-white">
-                                    <div className="mb-2 flex flex-wrap gap-1.5">
-                                        {item.tags.slice(0, 2).map((tag) => (
-                                            <Tag key={tag} variant="filled" className="m-0 bg-white/15 text-[11px] text-white backdrop-blur">
-                                                {tag}
-                                            </Tag>
-                                        ))}
-                                    </div>
-                                    <h3 className="text-sm font-medium">{item.title}</h3>
-                                    <p className="mt-1 line-clamp-2 text-xs leading-5 text-white/75">{item.prompt}</p>
-                                </div>
-                            </button>
-                        ))}
-                    </div>
-                </section>
             </section>
-            <Image.PreviewGroup
-                preview={{
-                    open: previewOpen,
-                    current: previewIndex,
-                    onOpenChange: setPreviewOpen,
-                    onChange: setPreviewIndex,
-                }}
-            >
-                <div className="hidden">
-                    {promptShowcase.map((item) => (
-                        <Image key={item.id} src={item.coverUrl} alt={item.title} />
-                    ))}
-                </div>
-            </Image.PreviewGroup>
         </main>
     );
 }
diff --git a/web/src/services/api/prompts.ts b/web/src/services/api/prompts.ts
index 291aeb0..2450005 100644
--- a/web/src/services/api/prompts.ts
+++ b/web/src/services/api/prompts.ts
@@ -28,11 +28,41 @@ export type PromptListResponse = {
     total: number;
 };
 
-const awesomeGptImageRawBase = "https://raw.githubusercontent.com/ZeroLu/awesome-gpt-image/main";
-const awesomeGpt4oImagePromptsBase = "https://raw.githubusercontent.com/ImgEdify/Awesome-GPT4o-Image-Prompts/main";
-const youMindGptImage2RawBase = "https://raw.githubusercontent.com/YouMind-OpenLab/awesome-gpt-image-2/main";
-const youMindNanoBananaProRawBase = "https://raw.githubusercontent.com/YouMind-OpenLab/awesome-nano-banana-pro-prompts/main";
-const davidWuGptImage2RawBase = "https://raw.githubusercontent.com/davidwuw0811-boop/awesome-gpt-image2-prompts/main";
+// [dokng] GitHub raw 域（raw.githubusercontent.com）国内基本不可达，导致提示词 markdown /
+// 封面图拉取失败、图片显示不出来。统一改走 jsDelivr 的 GitHub 镜像（cdn.jsdelivr.net/gh），
+// 把 raw.githubusercontent.com/{owner}/{repo}/{ref}/... 重写为 cdn.jsdelivr.net/gh/{owner}/{repo}@{ref}/...。
+// 应用到下方 5 个 base 常量（markdown / json / 拼接图片全部经此）与 absoluteImage（兜住
+// markdown 内嵌的绝对 raw 链接）。非 raw 域链接改走图片代理（见 proxyImage），不再原样返回。
+function toCdn(url: string) {
+    return url.replace(/^https:\/\/raw\.githubusercontent\.com\/([^/]+)\/([^/]+)\/([^/]+)(\/.*)?$/, "https://cdn.jsdelivr.net/gh/$1/$2@$3$4");
+}
+
+// [dokng] 提示词封面图分布在多种图床：GitHub raw（走 jsDelivr）、Twitter（pbs.twimg.com）、
+// 各源私有 CDN（cdn.imgedify.com / cms-assets.youmind.com 等）。后两类 jsDelivr 帮不上（它只镜像
+// GitHub 仓库文件，不代理任意外链），国内多不可达。故对「非 GitHub raw 的绝对图片链接」统一套一层
+// 图片代理前缀，借代理可达 + 缓存把图拉回来。
+//
+// 代理前缀做成可配置常量：默认用公共的 images.weserv.nl（免费、带边缘缓存、支持 ?url= 透传）。
+// 待自建 Cloudflare Worker 反代（挂在 dl.dokng.com 同账号，如 https://img.dokng.com/?url=）就绪后，
+// 把 IMAGE_PROXY 换成自建前缀即可——调用方无需改动。注意 weserv 对部分强防盗链图床（如 Twitter）
+// 仍可能失败，自建反代最稳。设为空串可整体关闭代理（回退原样返回）。
+const IMAGE_PROXY = "https://images.weserv.nl/?url=";
+
+// 把绝对图片链接改写为经代理可达的链接。GitHub raw 走 jsDelivr（已足够快且稳），其余外链套 IMAGE_PROXY。
+// weserv 的 ?url= 接受不带协议的 host+path（http(s) 都按 https 回源），故剥掉协议再 encode。
+function proxyImage(url: string) {
+    if (/^https:\/\/raw\.githubusercontent\.com\//i.test(url)) return toCdn(url);
+    if (!IMAGE_PROXY) return url;
+    // 已经是代理 / jsDelivr 链接的不再二次包裹。
+    if (url.startsWith(IMAGE_PROXY) || /^https:\/\/cdn\.jsdelivr\.net\//i.test(url)) return url;
+    return `${IMAGE_PROXY}${encodeURIComponent(url.replace(/^https?:\/\//i, ""))}`;
+}
+
+const awesomeGptImageRawBase = toCdn("https://raw.githubusercontent.com/ZeroLu/awesome-gpt-image/main");
+const awesomeGpt4oImagePromptsBase = toCdn("https://raw.githubusercontent.com/ImgEdify/Awesome-GPT4o-Image-Prompts/main");
+const youMindGptImage2RawBase = toCdn("https://raw.githubusercontent.com/YouMind-OpenLab/awesome-gpt-image-2/main");
+const youMindNanoBananaProRawBase = toCdn("https://raw.githubusercontent.com/YouMind-OpenLab/awesome-nano-banana-pro-prompts/main");
+const davidWuGptImage2RawBase = toCdn("https://raw.githubusercontent.com/davidwuw0811-boop/awesome-gpt-image2-prompts/main");
 const cacheTtlMs = 1000 * 60 * 60;
 const promptCacheKey = "third-party-prompts";
 const promptCacheStore = localforage.createInstance({ name: "infinite-canvas", storeName: "prompt_cache" });
@@ -186,13 +216,31 @@ function firstMatch(value: string, pattern: RegExp) {
     return pattern.exec(value)?.[1] || "";
 }
 
+// [dokng] 抽取一个 markdown 片段里的所有图片链接，按出现顺序去重。
+// 两种写法都要认：① markdown 图片语法 ![alt](url)；② 原生 HTML <img src="url">。
+// 关键：ImgEdify / YouMind 等源几乎全部用 <img> 标签写图，只认 markdown 语法会解析出 0 张、
+// 整类无封面（这是「好多图显示不出来」的主因）。两者合并后统一过 absoluteImage（拼相对路径 + 代理）。
 function extractMarkdownImages(baseUrl: string, markdown: string) {
-    return Array.from(markdown.matchAll(/!\[[^\]]*]\(([^)]+)\)/g), (match) => absoluteImage(baseUrl, match[1])).filter(Boolean);
+    const urls: string[] = [];
+    for (const match of markdown.matchAll(/!\[[^\]]*]\(([^)\s]+)/g)) urls.push(match[1]);
+    for (const match of markdown.matchAll(/<img[^>]+src=["']([^"']+)["']/gi)) urls.push(match[1]);
+    const seen = new Set<string>();
+    const result: string[] = [];
+    for (const raw of urls) {
+        const abs = absoluteImage(baseUrl, raw);
+        if (abs && !seen.has(abs)) {
+            seen.add(abs);
+            result.push(abs);
+        }
+    }
+    return result;
 }
 
 function absoluteImage(baseUrl: string, image: string) {
     if (!image) return "";
-    if (/^https?:\/\//i.test(image)) return image;
+    // 绝对链接（markdown / HTML 内嵌）：GitHub raw 走 jsDelivr，其余外链套图片代理（见 proxyImage）。
+    if (/^https?:\/\//i.test(image)) return proxyImage(image);
+    // 相对路径：拼到源仓库 base（本身已是 jsDelivr），不再额外代理。
     return `${baseUrl}/${image.replace(/^\.?\//, "")}`;
 }
 
