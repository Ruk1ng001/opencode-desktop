# 推送发布门禁清单（US-013）

> 目标：在**所有工作完成并验证无误**后，一次性推送、由 git / CI 自动合并发布，
> 全程不手动解决冲突。本文档固化推送前的**前置门槛清单**与各项**实测结论**，
> 作为「是否可推送」的唯一判据。
>
> 铁律（AC#5 / FR-17）：**任一前置门槛未过，不执行推送。**

---

## 1. 前置门槛清单与实测结论

以下 6 项须全绿方可推送。实测环境：无头 aarch64 Linux 服务器（无 `gh`、无 GUI/Electron
运行时、无发布 token）。可脚本化项已实测；实机 QA 项如实标注「待真机」，不伪造。

| # | 门槛 | 判据 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| G1 | 双包 typecheck 通过 | desktop + app 各 `bun run typecheck`（tsgo -b）rc=0 | ✅ 已过 | 从干净基线重放后实测 desktop rc=0 / app rc=0 |
| G2 | 补丁从干净 submodule 干净重放 0 冲突 | 从 `brand/BASE_SHA` 按 `brand/patches/*.patch` NN 序逐个 `git apply --check` + apply 全部无冲突 | ✅ 已过 | 9 个磁盘补丁（01-08 + 10）全部 `CHECK OK` + `APPLIED`，无冲突 |
| G3 | CI 4 平台产物齐全 | `release.yml` `package` job 矩阵 4 项，平台↔产物类型严格对齐 | ✅ 已过（配置层） | 矩阵实测 4 项：mac-arm64 / mac-x64 / win-x64 / linux-x64（US-009/US-010 验收，见 `release-acceptance.md`）；**一次真实四平台发布产物汇总仍待 CI 实跑回填** |
| G4 | 敏感值不入库 | 跟踪文件无 `sk-` 明文密钥、无渠道 Secret；`brand.json` 无真实密钥 | ✅ 已过 | 全仓跟踪文件扫描无 `sk-<真实密钥>` 模式；`brand.json` 仅含非密钥品牌值（`defaultLocale` 等），密钥经 CI Secret 打包期注入 |
| G5 | 自动更新三平台实机验证通过 | 三平台各 1 次「装旧版→发新版→检测→下载→安装→启动新版」实机闭环 | ❌ **未过** | US-011：程序化预检全绿，但**三平台真机实测确未跑**（无头服务器做不到）。手册见 `self-update-e2e.md`，结论表待真机回填 |
| G6 | UI 审查（US-012.5）通过且发现问题已修复 | 账户面板 / AddKeyForm / 首启引导 / 深链导入框 / 中文文案的视觉·文案·交互·可访问性审查，问题清单全部关闭 | ❌ **未过** | US-012.5 **从未执行**：PRD 中该 story 的 8 条 AC 全为 `[ ]`，progress.md 无任何 US-012.5 记录 |

---

## 2. 推送决策

**结论：不推送。**

G5（自动更新三平台实机验证）与 G6（UI 审查 US-012.5）两项前置门槛未过：

- **G5** 需要真实的 macOS / Windows / Linux 桌面实机装旧版→触发更新→肉眼确认，
  当前无头 aarch64 Linux 服务器无 GUI / Electron 运行时 / 发布 token，物理上无法执行，
  更不能伪造「已通过」（与 US-011 确立的诚实处理原则一致）。
- **G6** 依赖 US-012.5 先行完成（PRD 优先级 12，排在 US-013 之前）。该 story 的验收项
  全部未勾选、无实现记录，UI 审查这一环尚未发生。

按 AC#5 / FR-17，任一门槛未过即不执行推送。技术门槛（G1/G2/G4）已实测全绿、G3 配置层就绪，
一旦 G5/G6 完成即满足全部推送条件。

---

## 3. 满足条件后的推送流程（G5/G6 通过后执行）

推送遵循分支安全（AC#3）与零人工冲突（AC#2 / FR-16）：

1. **不直接推 `main`**（除非明确授权）。先创建特性分支：
   ```sh
   git switch -c release/cx-<版本>   # 或 feat/us-013-push
   ```
2. **只暂存本轮真正变更的文件**，隔离会话前无关脏改动，绝不 `git add -A`。
   尤其注意父仓库常年存在的会话前脏文件（`brand/README.md` / `brand/brand.json` /
   `brand/patches.manifest` / `patch 02·04·10` 等半完成迁移态），提交前逐一核对归属。
3. 提交后推特性分支并设上游跟踪：
   ```sh
   git push -u origin release/cx-<版本>
   ```
4. 走现有 CI 自动合并发布流程（不手动 resolve 冲突）。**若跟随上游 / 发布过程出现人工
   冲突，视为设计缺陷（FR-15/FR-16）**：先把引发冲突的补丁重构为不冲突形态（迁独立文件 /
   改运行期兜底），再重推——不手动 resolve 后了事。
5. 发布产物齐全性以 `release-acceptance.md`（四平台矩阵）与 `self-update-e2e.md`
   （三平台自更新载体 + 元数据）为准；三平台自更新实机结论表四行全「通过」才把
   OLD/NEW 作为正式发布推送。

---

## 变更记录

| 故事 | 变更 |
| --- | --- |
| US-013 | 固化推送发布门禁清单：6 项前置门槛逐条实测（G1 双包 typecheck rc=0 / G2 九补丁 0 冲突重放 / G3 CI 四平台矩阵 / G4 敏感值不入库 均已过；G5 自更新三平台实机、G6 UI 审查 US-012.5 未过）。按 AC#5「任一门槛未过不推送」，**本轮不执行推送**；固化满足条件后的分支安全推送流程。 |
